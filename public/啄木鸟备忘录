分两个接口:
url:/

{'keywords': {'成考': {{}, {}, {}}, '作弊': {}}}


{'PublishNumlineChart': {'Time':[2014,2015], News':[10,20], 'weibo':[]},
  'SourceChart': {'News':10%, }

}

ETL （数据仓库技术)
ETL，是英文 Extract-Transform-Load 的缩写，用来描述将数据从来源端经过抽取（extract）、转换（transform）、加载（load）至目的端的过程。ETL一词较常用在数据仓库，但其对象并不限于数据仓库。

数据缓冲层：存爬下来的原始数据

数据明细层：表中经过滤筛选后得到的数据，数据分析时会用到

数据汇总层：不同表或不同操作之间的汇总作用

数据应用层：根据业务需求提供相应的数据

相当于是请求数据应用层而不是 数据缓冲层

Elasticsearch 数据索引